# Suiren Component

This folder is reserved for the Suiren model code and weights.

The saved component prediction used by the ensemble is expected at:

```text
submissions/suiren_inactive_tail_weighted_three_seed_predictions.csv
```

The corresponding pretrained checkpoint, if available, should be placed at:

```text
models/suiren/weights/model.pt
```
