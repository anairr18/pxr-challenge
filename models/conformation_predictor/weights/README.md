# Conformation Predictor Weights

Place `model.pt` here if you are reproducing conformation-aware inference or
fine-tuning.

The repository keeps this as a placeholder because the binary checkpoint is too
large for a lightweight submission pack.
